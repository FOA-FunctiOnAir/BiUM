<Project>
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

  <PropertyGroup>
    <AssemblyName>BiApp.$(MSBuildProjectName)</AssemblyName>
    <RootNamespace>BiApp.$(MSBuildProjectName)</RootNamespace>
  </PropertyGroup>
</Project>
